
################################################################################33
Prompt 1 - ANALISE BACKEND

Estou a trabalhar no projeto DLMCare, da UC de Laboratórios de Informática IV.

Quero que analises apenas o BACKEND do projeto.

Contexto:
- O projeto é um sistema de gestão para uma cadeia de oficinas de reparação de trotinetes.
- A stack definida é FastAPI + Python no backend, MySQL na persistência e Vue no frontend.
- Eu vou tratar apenas do backend.
- NÃO quero implementar frontend.
- NÃO quero implementar a base de dados.
- NÃO quero criar modelos SQLAlchemy.
- NÃO quero criar migrations Alembic.
- NÃO quero alterar scripts SQL.

Podes analisar:
- backend/app/main.py
- backend/app/config.py
- backend/app/database.py
- backend/app/routers/
- backend/app/schemas/
- backend/app/services/
- backend/app/auth/
- README.md
- docs/setup_inicial.txt
- docs/git_branching.txt
- o relatório, especialmente a secção da API REST e os casos de uso.

Quero que faças:
1. Estado atual do backend.
2. O que já está preparado.
3. O que falta implementar para a Etapa 3.
4. Lista de módulos backend a criar.
5. Ordem de implementação recomendada.
6. Riscos/incoerências que devo corrigir antes de programar.
7. Lista exata de ficheiros a criar/alterar.

Regras:
- Não escrevas código ainda.
- Não mexas na base de dados.
- Não mexas no frontend.
- A faturação deve seguir esta regra:
  valor final = preço do serviço + valor de venda das peças utilizadas.
- O custo de compra das peças é apenas interno e não deve ser usado para calcular o valor cobrado ao cliente.
###################################################################################################################

#######################################################################################################################
Prompt 2

docs/backend_api_contract_etapa3.md

Objetivo:
Definir o contrato da API backend do projeto DLMCare para a Etapa 3.

Contexto do projeto:
- DLMCare é um sistema de gestão para uma cadeia/rede de oficinas de reparação de trotinetes.
- O backend usa FastAPI.
- O frontend será tratado por outra pessoa.
- A base de dados será tratada por outra pessoa.
- Eu vou implementar apenas a camada backend aplicacional: routers, schemas, services, autenticação, validações, permissões, lógica de negócio mockada/stub e documentação.

Regras obrigatórias:
- NÃO implementar código.
- NÃO criar modelos SQLAlchemy.
- NÃO criar migrations Alembic.
- NÃO criar ou alterar scripts SQL.
- NÃO criar repositories reais com queries à BD.
- NÃO decidir estrutura física da base de dados.
- NÃO implementar frontend.
- Este ficheiro é apenas documentação do contrato da API.
- Sempre que algo depender da BD, marcar como “pendente de integração com BD”.
- A lógica pode ser preparada para services mockados/in-memory na Etapa 3.

Também quero que evites sugerir que a falta de BD bloqueia completamente o backend. A API deve poder ser implementada temporariamente com services mockados, para permitir desenvolvimento incremental.

Estrutura obrigatória do documento:

1. Introdução
   - Objetivo do contrato da API
   - Papel deste contrato na Etapa 3
   - Separação entre backend aplicacional e persistência
   - Explicação de que a BD será integrada posteriormente

2. Convenções gerais da API
   - Prefixo base: /api/v1
   - Formato JSON
   - Códigos HTTP usados
   - Formato padrão de sucesso
   - Formato padrão de erro
   - Paginação, quando aplicável
   - Autenticação via Bearer Token JWT

3. Perfis de utilizador e permissões
   Usar estes perfis:
   - ADMINISTRADOR
   - GERENTE_LOJA
   - RECECIONISTA
   - MECANICO

   Explicar:
   - ADMINISTRADOR pode aceder a dados globais
   - GERENTE_LOJA, RECECIONISTA e MECANICO devem ficar limitados à sua loja_id
   - loja_id pode ser null apenas para ADMINISTRADOR
   - As permissões devem ser aplicadas nas dependencies do backend

4. Autenticação
   Documentar estes endpoints:
   - POST /api/v1/auth/login
   - POST /api/v1/auth/refresh
   - GET /api/v1/auth/me

   Para cada endpoint, incluir:
   - método
   - path
   - descrição
   - perfis autorizados
   - request body JSON
   - response body JSON
   - erros possíveis
   - service associado
   - estado atual: mockado / pendente de BD / pronto para integração

   Decisão temporária:
   - Na Etapa 3, o login pode usar utilizadores mockados.
   - O refresh token deve ser stateless com JWT, por agora.
   - Não criar tabela de refresh tokens.

5. Clientes
   Documentar:
   - GET /api/v1/clientes?query={nif|telemovel}
   - POST /api/v1/clientes
   - GET /api/v1/clientes/{id}
   - GET /api/v1/clientes/{id}/historico

   Incluir validações:
   - NIF com 9 dígitos
   - telemóvel português com 9 dígitos
   - email válido, quando fornecido
   - consentimento RGPD obrigatório no registo

6. Trotinetes
   Documentar:
   - POST /api/v1/trotinetes
   - GET /api/v1/trotinetes/{id}
   - GET /api/v1/trotinetes?cliente_id={id}

   Incluir:
   - associação obrigatória a cliente
   - número de série único, validado no mock service por agora
   - marca, modelo e observações técnicas

7. Peças
   Documentar:
   - GET /api/v1/pecas
   - GET /api/v1/pecas/{id}
   - POST /api/v1/pecas

   A peça deve distinguir obrigatoriamente:
   - preco_custo: valor interno de compra/aquisição
   - preco_venda: valor cobrado ao cliente

   Regra:
   - preco_custo nunca deve ser usado para calcular o valor final da fatura do cliente.

8. Stock
   Documentar:
   - GET /api/v1/stock?loja_id={id}
   - POST /api/v1/stock/entradas
   - POST /api/v1/stock/transferencias

   Incluir:
   - stock por loja
   - limite mínimo
   - alerta quando quantidade <= limite_minimo
   - validação de stock suficiente em transferências
   - movimentos de stock devem gerar registo de auditoria mockado

9. Ordens de Serviço
   Documentar:
   - POST /api/v1/ordens-servico
   - GET /api/v1/ordens-servico/{id}
   - GET /api/v1/ordens-servico?loja_id={id}&estado={estado}
   - PATCH /api/v1/ordens-servico/{id}/estado
   - POST /api/v1/ordens-servico/{id}/tempos/iniciar
   - POST /api/v1/ordens-servico/{id}/tempos/parar
   - POST /api/v1/ordens-servico/{id}/pecas

   Definir estes estados:
   - PENDENTE
   - EM_DIAGNOSTICO
   - AGUARDA_APROVACAO
   - EM_REPARACAO
   - AGUARDA_PECAS
   - CONCLUIDA
   - FATURADA
   - CANCELADA

   Definir transições permitidas:
   - PENDENTE → EM_DIAGNOSTICO
   - EM_DIAGNOSTICO → AGUARDA_APROVACAO
   - AGUARDA_APROVACAO → EM_REPARACAO
   - EM_REPARACAO → AGUARDA_PECAS
   - AGUARDA_PECAS → EM_REPARACAO
   - EM_REPARACAO → CONCLUIDA
   - CONCLUIDA → FATURADA
   - qualquer estado antes de FATURADA pode passar para CANCELADA

   Notas importantes:
   - O registo de tempos serve para métricas internas.
   - O tempo de mão de obra NÃO deve ser usado diretamente para calcular o valor final da fatura.
   - A ordem de serviço deve guardar ou referenciar o preço comercial/tabelado do serviço.

10. Faturação
   Documentar:
   - POST /api/v1/faturas
   - GET /api/v1/faturas/{id}
   - GET /api/v1/faturas?ordem_servico_id={id}

   Regra de negócio obrigatória:
   valor_final = preco_servico + subtotal_pecas

   Onde:
   - preco_servico é o preço comercial/tabelado do serviço prestado.
   - subtotal_pecas = soma de quantidade * preco_venda_unitario.
   - preco_custo das peças é apenas interno.
   - tempo de mão de obra não entra diretamente no cálculo do valor final.
   - Não pode ser emitida fatura para OS que não esteja CONCLUIDA.
   - Não pode existir mais do que uma fatura para a mesma OS.

   A response da fatura deve discriminar:
   - id da fatura
   - ordem_servico_id
   - cliente
   - trotinete
   - serviço prestado
   - preco_servico
   - peças aplicadas
   - subtotal_pecas
   - valor_final
   - estado
   - data_emissao

11. Dashboard
   Documentar:
   - GET /api/v1/dashboard

   Métricas mockadas:
   - ordens por estado
   - ordens concluídas por loja
   - tempo médio de reparação
   - faturação total
   - peças abaixo do stock mínimo
   - eficiência por mecânico

12. Auditoria
   Documentar:
   - GET /api/v1/auditoria

   Logs mockados:
   - login
   - alteração de estado da OS
   - movimentação de stock
   - emissão de fatura
   - tentativa de acesso negada

13. Tabela-resumo final
   Criar uma tabela com:
   - Módulo
   - Endpoints
   - Perfil autorizado
   - Service associado
   - Estado atual
   - Dependência futura da BD

14. Notas para implementação backend na Etapa 3
   Explicar:
   - Primeiro devem ser criados schemas Pydantic.
   - Depois auth/JWT/RBAC.
   - Depois routers e services mockados.
   - Depois testes unitários e de integração com TestClient.
   - A integração com BD fica para depois/outro elemento.
   - Os services devem ser desenhados para serem facilmente ligados a repositories reais no futuro.

15. Fora de âmbito
   Explicitar que nesta fase ficam fora do meu trabalho:
   - frontend
   - modelos ORM
   - migrations
   - scripts SQL
   - repositories reais com queries
   - integração com software certificado de faturação

Formato:
- Markdown limpo.
- Tabelas sempre que fizer sentido.
- Exemplos JSON para os endpoints principais.
- Não escrever código Python.
- Não implementar nada.


##############################################################################################################################

##############################################################################

Prompt 3

Implementa apenas os schemas Pydantic e enums do backend DLMCare, com base no ficheiro:

docs/backend_api_contract_etapa3.md

Contexto:
- O projeto DLMCare é um sistema de gestão para uma cadeia de oficinas de reparação de trotinetes.
- A Etapa 3 será feita no backend FastAPI.
- O backend atual tem apenas infraestrutura base: main.py, config.py, database.py, /health e diretórios vazios.
- O contrato da API já está definido em docs/backend_api_contract_etapa3.md.
- Nesta tarefa quero criar apenas a camada de schemas/DTOs e enums, que será usada depois pelos routers e services.

Objetivo desta tarefa:
Criar todos os schemas Pydantic necessários para suportar o contrato da API da Etapa 3.

Regras obrigatórias:
- Implementar apenas ficheiros em backend/app/schemas/.
- NÃO implementar routers.
- NÃO implementar services.
- NÃO implementar repositories.
- NÃO implementar autenticação/JWT.
- NÃO alterar main.py.
- NÃO alterar config.py.
- NÃO alterar database.py.
- NÃO criar modelos SQLAlchemy.
- NÃO criar migrations Alembic.
- NÃO criar ou alterar scripts SQL.
- NÃO implementar frontend.
- NÃO inventar endpoints novos fora do contrato.
- Os schemas devem estar alinhados com docs/backend_api_contract_etapa3.md.
- Usar Pydantic v2.

Criar/alterar os seguintes ficheiros:

backend/app/schemas/__init__.py
backend/app/schemas/common.py
backend/app/schemas/auth.py
backend/app/schemas/utilizador.py
backend/app/schemas/loja.py
backend/app/schemas/cliente.py
backend/app/schemas/trotinete.py
backend/app/schemas/peca.py
backend/app/schemas/stock.py
backend/app/schemas/ordem_servico.py
backend/app/schemas/fatura.py
backend/app/schemas/dashboard.py
backend/app/schemas/auditoria.py

1. common.py

Criar schemas e utilitários comuns:

- ErrorResponse
  - detail: str
  - code: str | None

- SuccessResponse genérico, se fizer sentido
  - data
  - message

- PaginatedResponse genérico, se fizer sentido
  - data
  - total
  - page
  - page_size
  - pages

- PaginationParams
  - page: int = 1
  - page_size: int = 20
  - validar page >= 1
  - validar 1 <= page_size <= 100

- BaseConfig comum para from_attributes=True, se fizer sentido em Pydantic v2.

Também podes criar validators reutilizáveis ou funções auxiliares para:
- NIF português com 9 dígitos
- telemóvel português com 9 dígitos e começado por 9
- valores monetários >= 0
- quantidades positivas

2. Enums obrigatórios

Criar os enums nos ficheiros mais adequados, evitando duplicação:

- PerfilUtilizador:
  - ADMINISTRADOR
  - GERENTE_LOJA
  - RECECIONISTA
  - MECANICO

- EstadoOrdemServico:
  - PENDENTE
  - EM_DIAGNOSTICO
  - AGUARDA_APROVACAO
  - EM_REPARACAO
  - AGUARDA_PECAS
  - CONCLUIDA
  - FATURADA
  - CANCELADA

- PrioridadeOrdemServico:
  - BAIXA
  - NORMAL
  - ALTA
  - URGENTE

- EstadoFatura:
  - EMITIDA
  - ANULADA

- CategoriaPeca:
  - BATERIA
  - PNEU
  - TRAVAO
  - MOTOR
  - CONTROLADOR
  - LUZ
  - ACESSORIO
  - OUTRO

- TipoMovimentoStock:
  - ENTRADA
  - TRANSFERENCIA
  - SAIDA_REPARACAO

- TipoEventoAuditoria:
  - LOGIN_SUCESSO
  - LOGIN_FALHA
  - ACESSO_NEGADO
  - OS_ESTADO_ALTERADO
  - STOCK_ENTRADA
  - STOCK_TRANSFERENCIA
  - FATURA_EMITIDA

3. auth.py

Criar schemas para:

- LoginRequest
  - email
  - password

- TokenResponse
  - access_token
  - refresh_token
  - token_type = "bearer"
  - expires_in
  - user

- RefreshTokenRequest
  - refresh_token

- RefreshTokenResponse
  - access_token
  - refresh_token
  - token_type
  - expires_in

- CurrentUserResponse ou AuthUserResponse
  - id
  - nome
  - email
  - perfil
  - loja_id
  - loja_nome
  - ativo

Validar email com EmailStr.

4. utilizador.py

Criar schemas para representar utilizadores autenticados e utilizadores internos:

- UtilizadorBase
- UtilizadorResponse
- UtilizadorResumo

Campos:
- id
- nome
- email
- perfil
- loja_id
- loja_nome
- ativo

Notas:
- ADMINISTRADOR pode ter loja_id = None.
- Outros perfis devem ter loja_id, mas esta regra pode ficar comentada como validação futura se for melhor não bloquear já os schemas.

5. loja.py

Criar schemas para lojas:

- LojaBase
- LojaResponse
- LojaResumo

Campos:
- id
- nome
- morada
- telefone
- ativo

6. cliente.py

Criar schemas para:

- ClienteCreate
- ClienteResponse
- ClienteDetalheResponse
- ClienteResumo
- ClienteHistoricoItem
- ClienteHistoricoResponse, se necessário

Campos principais:
- id
- nome
- nif
- telemovel
- email
- morada
- consentimento_rgpd
- data_registo
- loja_id

Validações obrigatórias:
- nif: string com exatamente 9 dígitos numéricos.
- telemovel: string com exatamente 9 dígitos e começa por 9.
- email: EmailStr | None.
- consentimento_rgpd: no ClienteCreate tem de ser true. Se for false, deve falhar validação.
- nome obrigatório e não vazio.

7. trotinete.py

Criar schemas para:

- TrotineteCreate
- TrotineteResponse
- TrotineteDetalheResponse
- TrotineteResumo

Campos:
- id
- cliente_id
- marca
- modelo
- numero_serie
- ano_compra
- cor
- observacoes_tecnicas
- data_registo

Validações:
- cliente_id obrigatório e positivo.
- marca, modelo e numero_serie obrigatórios e não vazios.
- ano_compra opcional, mas se existir deve estar num intervalo plausível, por exemplo 2000 a 2100.

8. peca.py

Criar schemas para:

- PecaCreate
- PecaResponse
- PecaDetalheResponse
- PecaResumo

Campos:
- id
- referencia
- nome
- categoria
- descricao
- unidade
- preco_custo
- preco_venda
- ativo

Regra crítica:
- preco_custo é interno.
- preco_venda é o valor usado para faturar o cliente.
- PecaResponse e PecaResumo NÃO devem expor preco_custo em respostas públicas.
- PecaCreate pode receber preco_custo e preco_venda.
- preco_custo >= 0.
- preco_venda >= 0.
- referencia e nome obrigatórios.

9. stock.py

Criar schemas para:

- StockItemResponse
- StockEntradaRequest
- StockEntradaResponse
- StockTransferenciaRequest
- StockTransferenciaResponse

Campos conforme o contrato:
- peca_id
- peca_referencia
- peca_nome
- loja_id
- loja_nome
- quantidade
- limite_minimo
- alerta

Validações:
- quantidade em entradas/transferências tem de ser > 0.
- loja_origem_id e loja_destino_id devem ser diferentes na transferência.
- ids devem ser positivos.
- alerta será calculado depois no service, mas o schema deve permitir o campo bool.

10. ordem_servico.py

Criar schemas para:

- OrdemServicoCreate
- OrdemServicoResponse
- OrdemServicoResumo
- OrdemServicoDetalheResponse
- OrdemServicoEstadoUpdate
- OrdemServicoEstadoUpdateResponse
- TempoInicioResponse
- TempoParagemResponse
- PecaAplicadaRequest
- PecaAplicadaResponse
- PecaAplicadaResumo

Campos principais:
- id
- numero
- trotinete_id
- cliente_id
- loja_id
- mecanico_id
- estado
- prioridade
- descricao_problema
- preco_servico
- data_entrada
- data_conclusao
- pecas_aplicadas
- subtotal_pecas
- valor_estimado_total
- tempo_total_minutos
- fatura_id

Validações:
- trotinete_id positivo.
- loja_id positivo.
- mecanico_id opcional mas, se existir, positivo.
- descricao_problema obrigatória e não vazia.
- preco_servico >= 0.
- quantidade de peça > 0.
- novo_estado deve ser EstadoOrdemServico.
- Não implementar as transições de estado aqui; isso ficará no service.
- O registo de tempos é apenas para métricas internas e não deve ser usado em schemas de faturação como base de cálculo.

11. fatura.py

Criar schemas para:

- FaturaCreateRequest
- FaturaResponse
- FaturaResumo
- FaturaClienteInfo
- FaturaTrotineteInfo
- FaturaServicoInfo
- FaturaPecaAplicada
- FaturaLojaInfo

Regra de negócio obrigatória a refletir nos nomes/campos:
valor_final = preco_servico + subtotal_pecas

Campos:
- id
- numero
- ordem_servico_id
- data_emissao
- estado
- cliente
- trotinete
- servico
- pecas_aplicadas
- subtotal_pecas
- valor_final
- loja

Atenção:
- Não incluir custo de compra/preco_custo em nenhum schema público de fatura.
- Não incluir cálculo por tempo de mão de obra.
- preco_servico é o preço comercial/tabelado do serviço.
- subtotal_pecas é soma de quantidade * preco_venda_unitario.
- valor_final é calculado depois no service, mas o schema deve representar claramente esse resultado.

Validações:
- ordem_servico_id positivo.
- valores monetários >= 0.
- quantidade de peças > 0.

12. dashboard.py

Criar schemas para a resposta do dashboard:

- DashboardPeriodo
- OrdensConcluidasPorLoja
- PecaAbaixoStockMinimo
- EficienciaMecanico
- DashboardResponse

Campos conforme o contrato:
- periodo
- ordens_por_estado
- ordens_concluidas_por_loja
- tempo_medio_reparacao_minutos
- faturacao_total
- pecas_abaixo_stock_minimo
- eficiencia_por_mecanico

Validações:
- valores numéricos não negativos.

13. auditoria.py

Criar schemas para:

- AuditoriaDetalhe
- AuditoriaItemResponse
- AuditoriaResponse, se necessário

Campos:
- id
- evento
- descricao
- utilizador_id
- utilizador_nome
- loja_id
- ip_origem
- timestamp
- detalhe

O campo detalhe pode ser dict[str, Any].

14. __init__.py

Atualizar backend/app/schemas/__init__.py para exportar os principais schemas e enums de forma organizada.

15. Requisitos técnicos

- Usar Pydantic v2.
- Usar Field para descrições, exemplos e constraints.
- Usar EmailStr onde aplicável.
- Usar datetime para timestamps.
- Usar date quando o contrato indicar apenas data.
- Usar Decimal para valores monetários, se fizer sentido.
- Se usares Decimal, garante que os exemplos JSON continuam legíveis.
- Usar ConfigDict(from_attributes=True) quando fizer sentido para futura integração com ORM, sem criar modelos ORM.
- Evitar lógica de negócio complexa nos schemas.
- Validações simples de formato podem ficar nos schemas.
- Regras de negócio como transições de estado, duplicados, permissões e cálculo efetivo da fatura ficam para os services.

16. Cuidados especiais

- Não criar schemas com nomes contraditórios ao contrato.
- Não usar EstadoOrdemServico antigo como ABERTA ou AGUARDAR_PECAS. Usar exatamente AGUARDA_PECAS, como no contrato.
- Não criar perfil GERENTE. Usar exatamente GERENTE_LOJA.
- Não expor preco_custo em PecaResponse público nem em FaturaResponse.
- Não colocar tempo_total_minutos como base de cálculo da fatura.
- Não criar qualquer dependência da base de dados.
- Os schemas devem importar uns dos outros apenas quando fizer sentido e sem causar imports circulares.

17. No final da resposta, apresenta:

- Lista de ficheiros criados/alterados.
- Pequena explicação do papel de cada ficheiro.
- Exemplos JSON de:
  1. ClienteCreate
  2. TrotineteCreate
  3. OrdemServicoCreate
  4. PecaAplicadaRequest
  5. FaturaResponse
- Indicação clara de que routers, services, repositories, auth e BD ainda não foram implementados nesta tarefa.
- Comandos para verificar sintaxe/imports, por exemplo:
  python -m compileall backend/app/schemas
  
  
  #############################################################################################################


##############################################################################

Prompt 4 — Auth / JWT / RBAC

Implementa a camada de autenticação e controlo de acesso do backend DLMCare.

Contexto:
- Os schemas Pydantic já estão implementados em backend/app/schemas/.
- O contrato da API está em docs/backend_api_contract_etapa3.md.
- As dependências disponíveis são: python-jose[cryptography], passlib[bcrypt], fastapi, pydantic-settings.
- A configuração JWT (JWT_SECRET_KEY, JWT_ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES, REFRESH_TOKEN_EXPIRE_DAYS) já está em app/config.py via pydantic-settings.

Regras obrigatórias:
- NÃO implementar routers.
- NÃO implementar services.
- NÃO criar modelos SQLAlchemy.
- NÃO criar migrations Alembic.
- NÃO criar ou alterar scripts SQL.
- NÃO implementar frontend.
- NÃO criar repositories reais com queries à BD.
- NÃO alterar main.py, config.py, database.py nem nenhum schema.

Criar os seguintes ficheiros:

1. backend/app/core/__init__.py  (vazio)

2. backend/app/core/security.py

   Funções a implementar:
   - hash_password(password: str) -> str         usando passlib bcrypt
   - verify_password(plain: str, hashed: str) -> bool  usando passlib bcrypt
   - create_access_token(payload: dict) -> str   JWT com type="access", exp = now + ACCESS_TOKEN_EXPIRE_MINUTES
   - create_refresh_token(payload: dict) -> str  JWT com type="refresh", exp = now + REFRESH_TOKEN_EXPIRE_DAYS
   - decode_token(token: str, expected_type: str = "access") -> dict
     - Usa jose.jwt.decode com JWT_SECRET_KEY e JWT_ALGORITHM
     - Lança HTTPException 401 com code TOKEN_EXPIRED se JWTError ou type errado

   O payload dos tokens deve incluir:
   - sub: str (user_id como string)
   - nome: str
   - email: str
   - perfil: str (valor do enum PerfilUtilizador)
   - loja_id: int | None
   - loja_nome: str | None
   - ativo: bool
   - type: "access" | "refresh"
   - exp: datetime
   - iat: datetime

3. backend/app/auth/dependencies.py

   Dependências FastAPI a implementar:

   a) get_current_user(credentials: HTTPAuthorizationCredentials = Depends(HTTPBearer())) -> CurrentUserResponse
      - Chama decode_token com expected_type="access"
      - Constrói e devolve CurrentUserResponse a partir das claims do payload
      - Lança 401 se "sub" estiver ausente

   b) require_roles(*perfis: PerfilUtilizador)
      - Factory que devolve uma dependency FastAPI
      - A dependency verifica que current_user.perfil está em perfis
      - Lança 403 com code PERMISSION_DENIED se não estiver

   c) get_loja_context(loja_id: int | None = None, current_user = Depends(get_current_user)) -> int | None
      - ADMINISTRADOR: devolve o loja_id do query param (pode ser None para ver tudo)
      - Outros perfis: ignora o query param e devolve sempre current_user.loja_id
      - Se o query param loja_id for fornecido e diferente de current_user.loja_id (para não-ADMIN), lança 403 com code LOJA_MISMATCH

Notas:
- Usar HTTPBearer (não OAuth2PasswordBearer) pois o login é JSON, não form data.
- Os erros de autenticação/autorização devem seguir o formato {"detail": "...", "code": "..."} do contrato.
- get_current_user extrai o utilizador exclusivamente do JWT — sem acesso à BD.
- Neste passo os services mockados ainda não existem; routers e services serão implementados nos passos seguintes.

No final da resposta, apresenta:
- Lista de ficheiros criados com explicação de cada um.
- Comando para verificar sintaxe:
  python -m compileall backend/app/core backend/app/auth
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

Prompt 5 — Router + Service: Auth

Implementa o router e o service de autenticação do backend DLMCare.

Contexto:
- Os schemas estão em backend/app/schemas/auth.py e utilizador.py.
- O contrato dos endpoints está em docs/backend_api_contract_etapa3.md, secção 4.
- As utilities JWT e de password estão em app/core/security.py.
- As dependencies FastAPI (get_current_user, require_roles, get_loja_context) estão em app/auth/dependencies.py.
- passlib 1.7.4 é INCOMPATÍVEL com bcrypt >= 5.x. Usar bcrypt diretamente em security.py:
    import bcrypt as _bcrypt
    def hash_password(p): return _bcrypt.hashpw(p.encode(), _bcrypt.gensalt()).decode()
    def verify_password(plain, hashed): return _bcrypt.checkpw(plain.encode(), hashed.encode())

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/auth_service.py

   Lista mockada de utilizadores em memória [pendente de integração com BD].
   Incluir um utilizador de cada perfil:
   - ADMINISTRADOR: admin@dlmcare.pt / admin123
   - GERENTE_LOJA:  gerente.porto@dlmcare.pt / gerente123
   - RECECIONISTA:  ana.lisboa@dlmcare.pt / password123
   - MECANICO:      joao.mecanico@dlmcare.pt / mecanico123

   Hashes bcrypt pré-calculados (bcrypt 5.x, cost 12):
   - admin123:     $2b$12$Ljzvo8.JjstrAfsohpMqauw4OdBbLNksDIvP5eKKHaBEWRS5c4t4K
   - password123:  $2b$12$fI2Bd1BsD7iZYQvzuKojZu2R/9A5IsqjyqQajWj4Zv1H7wuY/Rkwa
   - gerente123:   $2b$12$d1NsqHAUS7myTvUEz3pef.Xfvpa5Du7.43v9zjIJVJek8t97LWC0G
   - mecanico123:  $2b$12$97e79p34JpiM2uZbSjJbvu3j6Q3HJtD1FSA67mNC8HmoVT5PKQOru

   Funções a implementar:
   - login(email, password) -> TokenResponse
     - Pesquisa utilizador por email (case-insensitive)
     - Verifica password com verify_password
     - Se email não existe ou password errada: 401 INVALID_CREDENTIALS
     - Se utilizador inativo: 403 ACCOUNT_INACTIVE
     - Cria access_token e refresh_token com create_access_token / create_refresh_token
     - Payload JWT: sub (str do id), nome, email, perfil (str), loja_id, loja_nome, ativo
     - expires_in = ACCESS_TOKEN_EXPIRE_MINUTES * 60

   - refresh(refresh_token) -> RefreshTokenResponse
     - Chama decode_token com expected_type="refresh"
     - Verifica que o utilizador ainda existe e está ativo
     - Emite novo par de tokens
     - Se inválido: 401 TOKEN_INVALID

2. backend/app/routers/auth.py

   Router FastAPI com prefix="/auth", tags=["autenticação"].

   Endpoints:
   - POST /login      → status 201, sem autenticação exigida
   - POST /refresh    → status 200, sem autenticação exigida
   - GET  /me         → status 200, requer get_current_user

3. backend/app/main.py

   Descomentar e adicionar:
     from app.routers import auth
     app.include_router(auth.router, prefix="/api/v1")

Notas:
- O GET /me devolve diretamente o CurrentUserResponse do get_current_user — sem BD.
- O router não importa nada da BD.
- Erros seguem o formato {"detail": "...", "code": "..."}.

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/auth_service.py backend/app/routers/auth.py backend/app/main.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

Prompt 6 — Router + Service: Clientes

Implementa o router e o service de clientes do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/cliente.py: ClienteCreate, ClienteResponse, ClienteDetalheResponse, ClienteResumo, ClienteHistoricoItem, TrotineteResumoEmCliente.
- Schemas comuns: DataResponse[T], PaginatedResponse[T] em schemas/common.py.
- Dependencies: get_current_user, require_roles em app/auth/dependencies.py.
- Contrato em docs/backend_api_contract_etapa3.md, secção 5.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/cliente_service.py

   Dados mockados em memória [pendente de integração com BD].
   Incluir 2 clientes de teste (loja_id=1).

   Funções a implementar:

   - listar(query, page, page_size, current_user) -> PaginatedResponse[ClienteResponse]
     - Filtro de loja automático: não-ADMIN vê só clientes da sua loja_id
     - query (opcional): exact match por NIF ou telemóvel
     - Paginação manual sobre a lista filtrada

   - criar(body: ClienteCreate, current_user) -> DataResponse[ClienteResponse]
     - Verifica NIF duplicado → 409 DUPLICATE_ENTRY
     - loja_id efectiva: current_user.loja_id ou 1 como fallback para ADMIN sem loja
     - Adiciona à lista em memória
     - message: "Cliente registado com sucesso."

   - obter(cliente_id, current_user) -> DataResponse[ClienteDetalheResponse]
     - 404 RESOURCE_NOT_FOUND se não existe
     - 403 LOJA_MISMATCH se não-ADMIN tenta aceder a cliente de outra loja
     - trotinetes=[]: será populado quando trotinetes forem implementadas

   - historico(cliente_id, page, page_size, current_user) -> PaginatedResponse[ClienteHistoricoItem]
     - Mesma verificação de acesso que obter
     - Devolve lista vazia [pendente de integração com OS]

   Helper interno:
   - _check_loja(cliente, current_user): ADMIN passa sempre; outros levantam 403 se loja_id diferente

2. backend/app/routers/clientes.py

   Router FastAPI com prefix="/clientes", tags=["clientes"].

   Endpoints:
   - GET  /clientes                  → todos os perfis
   - POST /clientes                  → ADMIN, GERENTE_LOJA, RECECIONISTA (status 201)
   - GET  /clientes/{cliente_id}     → todos os perfis
   - GET  /clientes/{cliente_id}/historico → todos os perfis

   Usar require_roles() para RBAC. Não usar get_loja_context — a filtragem de loja
   é feita dentro do service com base em current_user.

3. backend/app/main.py

   Adicionar:
     from app.routers import auth, clientes
     app.include_router(clientes.router, prefix="/api/v1")

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/cliente_service.py backend/app/routers/clientes.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

Prompt 7 — Router + Service: Trotinetes

Implementa o router e o service de trotinetes do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/trotinete.py: TrotineteCreate, TrotineteResponse, TrotineteDetalheResponse, TrotineteResumo, ClienteResumoEmTrotinete.
- Schemas comuns: DataResponse[T], PaginatedResponse[T] em schemas/common.py.
- Dependencies: require_roles em app/auth/dependencies.py.
- Contrato em docs/backend_api_contract_etapa3.md, secção 6.
- cliente_service._find(id) está disponível para validar a existência do cliente.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/trotinete_service.py

   Dados mockados em memória [pendente de integração com BD].
   Incluir 2 trotinetes de teste (loja_id=1, associadas aos clientes mockados).
   Guardar loja_id internamente no dict (para controlo de acesso), mas não expor
   no TrotineteResponse (excluir com {k: v for k, v in t.items() if k != "loja_id"}).

   Funções:
   - listar(cliente_id, numero_serie, page, page_size, current_user) -> PaginatedResponse[TrotineteResponse]
     - Filtro de loja automático para não-ADMIN
     - Filtro opcional por cliente_id e numero_serie (exact match)

   - criar(body, current_user) -> DataResponse[TrotineteResponse]
     - Import local de cliente_service para validar cliente_id (evitar circular import)
     - 404 se cliente não existe
     - 403 LOJA_MISMATCH se cliente pertence a outra loja
     - 409 DUPLICATE_ENTRY se numero_serie já existe
     - loja_id da trotinete herdada do cliente

   - obter(trotinete_id, current_user) -> DataResponse[TrotineteDetalheResponse]
     - 404 / 403 como esperado
     - Import local de cliente_service para construir ClienteResumoEmTrotinete
     - total_ordens=0 [pendente OS]

   - get_por_cliente(cliente_id) -> list[dict]  (helper interno, sem autenticação)
     - Usado por cliente_service.obter para popular a lista de trotinetes

2. backend/app/routers/trotinetes.py

   Router com prefix="/trotinetes", tags=["trotinetes"].

   Endpoints:
   - GET  /trotinetes                → todos os perfis
   - POST /trotinetes                → ADMIN, GERENTE_LOJA, RECECIONISTA (status 201)
   - GET  /trotinetes/{trotinete_id} → todos os perfis

3. backend/app/services/cliente_service.py  (actualizar obter)

   Substituir trotinetes=[] por import local de trotinete_service.get_por_cliente
   para popular a lista de trotinetes no ClienteDetalheResponse.
   Usar import local (dentro da função) para evitar dependência circular.

4. backend/app/main.py

   Adicionar:
     from app.routers import auth, clientes, trotinetes
     app.include_router(trotinetes.router, prefix="/api/v1")

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/trotinete_service.py backend/app/routers/trotinetes.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

Prompt 8 — Router + Service: Peças

Implementa o router e o service de peças do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/peca.py: PecaCreate, PecaResponse, PecaDetalheResponse, PecaResumo, CategoriaPeca.
- PecaResponse NÃO inclui preco_custo — campo interno nunca exposto em respostas públicas.
- Pydantic v2 ignora campos extra por defeito, por isso PecaResponse(**dict_com_preco_custo) funciona.
- Schemas comuns: DataResponse[T], PaginatedResponse[T].
- Dependencies: require_roles em app/auth/dependencies.py.
- Contrato em docs/backend_api_contract_etapa3.md, secção 7.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.
- preco_custo NUNCA deve aparecer em respostas públicas da API.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/peca_service.py

   Dados mockados em memória [pendente de integração com BD].
   Incluir 4 peças de teste cobrindo categorias diferentes (BATERIA, PNEU, TRAVAO, MOTOR).
   Armazenar preco_custo no dict interno mas nunca serializar no schema de resposta.

   Funções:
   - listar(query, categoria, page, page_size) -> PaginatedResponse[PecaResponse]
     - Filtra apenas peças ativas
     - query: substring match em nome e referencia (case-insensitive)
     - categoria: CategoriaPeca | None

   - obter(peca_id) -> DataResponse[PecaDetalheResponse]
     - 404 RESOURCE_NOT_FOUND se não existe

   - criar(body: PecaCreate) -> DataResponse[PecaResponse]
     - 409 DUPLICATE_ENTRY se referencia já existe
     - message: "Peça criada com sucesso."

   - get_peca_interna(peca_id) -> dict | None
     - Helper para uso interno por outros services (stock, OS, fatura)
     - Devolve o dict completo incluindo preco_custo

   Nota: o router de peças não recebe current_user nas funções do service (sem
   filtragem por loja — catálogo global). O current_user é usado apenas para RBAC no router.

2. backend/app/routers/pecas.py

   Router com prefix="/pecas", tags=["peças"].

   Endpoints:
   - GET  /pecas          → todos os perfis
   - GET  /pecas/{peca_id} → todos os perfis
   - POST /pecas          → ADMINISTRADOR, GERENTE_LOJA (status 201)

   O current_user é recebido como _ (não utilizado no service).

3. backend/app/main.py

   Adicionar:
     from app.routers import auth, clientes, trotinetes, pecas
     app.include_router(pecas.router, prefix="/api/v1")

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/peca_service.py backend/app/routers/pecas.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

Prompt 9 — Router + Service: Stock

Implementa o router e o service de stock do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/stock.py: StockItemResponse, StockEntradaRequest, StockEntradaResponse, StockTransferenciaRequest, StockTransferenciaResponse, TipoMovimentoStock.
- Schemas comuns: DataResponse[T], PaginatedResponse[T].
- Dependencies: require_roles em app/auth/dependencies.py.
- peca_service.get_peca_interna(peca_id) disponível para lookup de peças.
- Contrato em docs/backend_api_contract_etapa3.md, secção 8.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/stock_service.py

   Dados mockados em memória [pendente de integração com BD].
   Stock estruturado como lista de dicts {peca_id, loja_id, quantidade, limite_minimo}.
   Mock com 2 lojas: {1: "DLMCare Porto", 2: "DLMCare Lisboa"}.
   Incluir stock inicial para as 4 peças em loja 1 e 2 itens em loja 2.

   Funções:
   - listar(loja_id, apenas_alertas, page, page_size, current_user) -> PaginatedResponse[StockItemResponse]
     - não-ADMIN: ignora loja_id param, usa sempre current_user.loja_id
     - apenas_alertas=True: filtra quantidade <= limite_minimo
     - StockItemResponse inclui peca_referencia e peca_nome (lookup via peca_service)
     - alerta = quantidade <= limite_minimo (calculado no service)

   - entrada(body, current_user) -> DataResponse[StockEntradaResponse]
     - 403 LOJA_MISMATCH se não-ADMIN tenta dar entrada noutra loja
     - 404 se loja ou peça não existe
     - Cria registo de stock se não existir ainda para esse (peca_id, loja_id)
     - [pendente] log auditoria STOCK_ENTRADA

   - transferencia(body, current_user) -> DataResponse[StockTransferenciaResponse]
     - 403 LOJA_MISMATCH se não-ADMIN e loja_origem_id != current_user.loja_id
     - 404 se loja ou peça não existe
     - 400 INSUFFICIENT_STOCK se origem não tem quantidade suficiente
     - Actualiza quantidades de origem e destino
     - [pendente] log auditoria STOCK_TRANSFERENCIA

   Helpers internos expostos para outros services:
   - get_stock_disponivel(peca_id, loja_id) -> int
   - consumir_stock(peca_id, loja_id, quantidade) -> None  (lança 400 se insuficiente)

2. backend/app/routers/stock.py

   Router com prefix="/stock", tags=["stock"].

   Endpoints:
   - GET  /stock               → todos os perfis
   - POST /stock/entradas      → ADMINISTRADOR, GERENTE_LOJA (status 201)
   - POST /stock/transferencias → ADMINISTRADOR, GERENTE_LOJA (status 201)

3. backend/app/main.py

   Adicionar:
     from app.routers import ..., stock
     app.include_router(stock.router, prefix="/api/v1")

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/stock_service.py backend/app/routers/stock.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

Prompt 10 — Router + Service: Ordens de Serviço

Implementa o router e o service de ordens de serviço do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/ordem_servico.py: OrdemServicoCreate, OrdemServicoResponse,
  OrdemServicoResumo, OrdemServicoDetalheResponse, OrdemServicoEstadoUpdate,
  OrdemServicoEstadoUpdateResponse, PecaAplicadaRequest, PecaAplicadaResponse,
  PecaAplicadaResumo, TempoInicioResponse, TempoParagemResponse, EstadoOrdemServico,
  PrioridadeOrdemServico, _OSClienteInfo, _OSTrotineteInfo, _OSMecanicoInfo.
- Helpers disponíveis: trotinete_service._find, cliente_service._find,
  peca_service.get_peca_interna, stock_service.consumir_stock, stock_service._MOCK_LOJAS,
  auth_service._MOCK_USERS.
- Usar imports locais (dentro das funções) para evitar dependências circulares.
- Contrato em docs/backend_api_contract_etapa3.md, secção 9.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.
- preco_custo NUNCA deve aparecer em nenhum cálculo.
- preco_venda_unitario é snapshotted no momento de adicionar_peca.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/ordem_servico_service.py

   Mock em memória com 2 OS (uma PENDENTE, uma EM_REPARACAO com peça aplicada).
   Dict interno: {id, numero, trotinete_id, cliente_id, loja_id, mecanico_id, estado,
   prioridade, descricao_problema, preco_servico, data_entrada, data_conclusao,
   pecas_aplicadas: list[dict], tempo_total_minutos, inicio_tempo_atual, fatura_id}

   Tabela de transições _TRANSICOES: dict[(E_atual, E_novo), frozenset(perfis)]:
   - PENDENTE → EM_DIAGNOSTICO: ADMIN, GERENTE, MECANICO
   - PENDENTE → CANCELADA: ADMIN, GERENTE, RECECIONISTA
   - EM_DIAGNOSTICO → AGUARDA_APROVACAO: ADMIN, GERENTE, MECANICO
   - EM_DIAGNOSTICO → CANCELADA: ADMIN, GERENTE
   - AGUARDA_APROVACAO → EM_REPARACAO: ADMIN, GERENTE, RECECIONISTA
   - AGUARDA_APROVACAO → CANCELADA: ADMIN, GERENTE, RECECIONISTA
   - EM_REPARACAO → AGUARDA_PECAS: ADMIN, GERENTE, MECANICO
   - EM_REPARACAO → CONCLUIDA: ADMIN, GERENTE, MECANICO
   - EM_REPARACAO → CANCELADA: ADMIN, GERENTE
   - AGUARDA_PECAS → EM_REPARACAO: ADMIN, GERENTE, MECANICO
   - AGUARDA_PECAS → CANCELADA: ADMIN, GERENTE
   (CONCLUIDA → FATURADA: apenas via POST /faturas, não aqui)

   Funções:
   - criar(body, current_user) -> DataResponse[OrdemServicoResponse]
     - Valida trotinete existe; loja_id do body deve corresponder à loja da trotinete
     - Valida mecanico_id se fornecido (deve ser MECANICO da mesma loja)
     - cliente_id herdado da trotinete
     - numero = f"OS-2026-{id:04d}"

   - listar(loja_id, estado, mecanico_id, data_inicio, data_fim, page, page_size, current_user) -> PaginatedResponse[OrdemServicoResumo]
     - Filtro de loja automático para não-ADMIN
     - Filtros opcionais: estado, mecanico_id, data_inicio/fim (comparação de date)
     - Resumo inclui loja_nome, cliente_nome, trotinete_numero_serie, mecanico_nome (lookups locais)

   - obter(os_id, current_user) -> DataResponse[OrdemServicoDetalheResponse]
     - 404 / 403 padrão
     - Detalhe inclui objetos aninhados cliente, trotinete, mecanico
     - subtotal_pecas = Σ(subtotal) das pecas_aplicadas
     - valor_estimado_total = preco_servico + subtotal_pecas

   - atualizar_estado(os_id, body, current_user) -> DataResponse[OrdemServicoEstadoUpdateResponse]
     - Valida transição na tabela _TRANSICOES: 409 INVALID_STATE_TRANSITION se não existe
     - Valida perfil do utilizador: 403 PERMISSION_DENIED se não autorizado
     - Se novo_estado == CONCLUIDA: define data_conclusao = now()
     - [pendente] auditoria OS_ESTADO_ALTERADO

   - iniciar_tempo(os_id, current_user) -> DataResponse[TempoInicioResponse]
     - 409 se inicio_tempo_atual já definido
     - Guarda datetime.now() em inicio_tempo_atual

   - parar_tempo(os_id, current_user) -> DataResponse[TempoParagemResponse]
     - 409 se inicio_tempo_atual é None
     - Calcula minutos = (now - inicio) // 60
     - Acumula em tempo_total_minutos
     - Limpa inicio_tempo_atual

   - adicionar_peca(os_id, body, current_user) -> DataResponse[PecaAplicadaResponse]
     - 409 se OS em CONCLUIDA, FATURADA ou CANCELADA
     - 404 se peca não existe
     - Chama stock_service.consumir_stock (lança 400 INSUFFICIENT_STOCK se falhar)
     - Snapshot: preco_venda_unitario = peca["preco_venda"] (NUNCA preco_custo)
     - Se peca já na lista, acumula quantidade e subtotal
     - message: "Peça adicionada à ordem de serviço."

   Helper público:
   - get_os_interna(os_id) -> dict | None  (para fatura_service)

2. backend/app/routers/ordens_servico.py

   Router com prefix="/ordens-servico", tags=["ordens de serviço"].

   Endpoints:
   - POST  /ordens-servico                   → ADMIN, GERENTE, RECECIONISTA (201)
   - GET   /ordens-servico                   → todos os perfis
   - GET   /ordens-servico/{os_id}           → todos os perfis
   - PATCH /ordens-servico/{os_id}/estado    → get_current_user (RBAC no service)
   - POST  /ordens-servico/{os_id}/tempos/iniciar → ADMIN, GERENTE, MECANICO
   - POST  /ordens-servico/{os_id}/tempos/parar   → ADMIN, GERENTE, MECANICO
   - POST  /ordens-servico/{os_id}/pecas          → ADMIN, GERENTE, MECANICO (201)

3. backend/app/main.py — adicionar ordens_servico router

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/ordem_servico_service.py backend/app/routers/ordens_servico.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

##############################################################################

Prompt 11 — Router + Service: Faturas

Implementa o router e o service de faturação do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/fatura.py: FaturaCreateRequest, FaturaResponse, FaturaResumo,
  FaturaClienteInfo, FaturaTrotineteInfo, FaturaServicoInfo, FaturaPecaAplicada, FaturaLojaInfo, EstadoFatura.
- Schemas comuns: DataResponse[T], PaginatedResponse[T] em schemas/common.py.
- Dependencies: require_roles em app/auth/dependencies.py.
- Helpers disponíveis:
    - ordem_servico_service.get_os_interna(os_id) -> dict | None
    - cliente_service._find(id), trotinete_service._find(id), peca_service.get_peca_interna(id)
    - stock_service._MOCK_LOJAS: dict[int, str]
- Contrato em docs/backend_api_contract_etapa3.md, secção 10.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.
- preco_custo NUNCA deve aparecer em nenhum cálculo nem em nenhuma resposta.
- valor_final = preco_servico + subtotal_pecas (onde subtotal_pecas = Σ quantidade × preco_venda_unitario).

Criar/alterar os seguintes ficheiros:

1. backend/app/services/fatura_service.py

   Mock em memória: _MOCK_FATURAS: list[dict] = [], _next_id = 1.
   _MOCK_LOJAS_INFO: dict[int, dict] com nome, morada e telefone das 2 lojas (para imprimir na fatura).

   Funções:
   - emitir(ordem_servico_id, current_user) -> DataResponse[FaturaResponse]
     - Verificar existência da OS → 404 RESOURCE_NOT_FOUND
     - Verificar loja (LOJA_MISMATCH para não-ADMIN)
     - Verificar fatura_id != None ANTES de verificar estado → 409 ORDER_ALREADY_INVOICED
       (importante: esta verificação deve ser ANTES da verificação de estado, pois a OS
       já está em FATURADA e não CONCLUIDA após a primeira fatura emitida)
     - Verificar estado == CONCLUIDA → 400 ORDER_NOT_CONCLUDED
     - Construir linhas de peça: para cada peca em os["pecas_aplicadas"],
       lookup de referencia via peca_service.get_peca_interna
     - subtotal_pecas = Σ p["subtotal"] das pecas_linha
     - valor_final = round(preco_servico + subtotal_pecas, 2)
     - numero = f"FAT-2026-{id:04d}"
     - Atualizar os["fatura_id"] = id e os["estado"] = E.FATURADA
     - [pendente] auditoria FATURA_EMITIDA
     - message: "Fatura emitida com sucesso."

   - obter(fatura_id, current_user) -> DataResponse[FaturaResponse]
     - 404 se não existe
     - 403 LOJA_MISMATCH se não-ADMIN

   - listar(ordem_servico_id, loja_id, data_inicio, data_fim, page, page_size, current_user)
       -> PaginatedResponse[FaturaResumo]
     - Filtro de loja automático para não-ADMIN
     - Filtros opcionais: ordem_servico_id, data_inicio/fim (comparação de date)

2. backend/app/routers/faturas.py

   Router com prefix="/faturas", tags=["faturas"].
   Perfis autorizados: ADMINISTRADOR, GERENTE_LOJA, RECECIONISTA.

   Endpoints:
   - POST /faturas             → status 201
   - GET  /faturas/{fatura_id} → status 200
   - GET  /faturas             → status 200

3. backend/app/main.py

   Adicionar:
     from app.routers import ..., faturas
     app.include_router(faturas.router, prefix="/api/v1")

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/fatura_service.py backend/app/routers/faturas.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

##############################################################################

Prompt 12 — Router + Service: Dashboard

Implementa o router e o service de dashboard do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/dashboard.py: DashboardResponse, DashboardPeriodo,
  OrdensConcluidasPorLoja, PecaAbaixoStockMinimo, EficienciaMecanico.
- Schemas comuns: DataResponse[T] em schemas/common.py.
- Dependencies: require_roles em app/auth/dependencies.py.
- Dados disponíveis via imports locais (para evitar circulares):
    - ordem_servico_service._MOCK_OS
    - fatura_service._MOCK_FATURAS
    - stock_service._MOCK_STOCK, stock_service._MOCK_LOJAS
    - peca_service._MOCK_PECAS
    - auth_service._MOCK_USERS
    - app.schemas.ordem_servico.EstadoOrdemServico
- Contrato em docs/backend_api_contract_etapa3.md, secção 11.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.
- O service deve calcular as métricas a partir dos mocks em memória, não usar dados estáticos.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/dashboard_service.py

   Função única:
   - obter(loja_id, data_inicio, data_fim, current_user) -> DataResponse[DashboardResponse]

   Defaults do período:
   - data_inicio: hoje - 30 dias
   - data_fim: hoje

   Filtro de loja:
   - ADMINISTRADOR: usa loja_id do query param (None = todas as lojas)
   - Outros perfis: usa sempre current_user.loja_id

   Métricas a calcular:
   - ordens_por_estado: dict com todos os valores de EstadoOrdemServico como chaves (zero se ausente)
   - ordens_concluidas_por_loja: OS com estado CONCLUIDA ou FATURADA, agrupadas por loja_id
   - tempo_medio_reparacao_minutos: média de tempo_total_minutos das OS com valor definido (None se nenhuma)
   - faturacao_total: soma de valor_final das faturas emitidas no período (filtradas por loja se aplicável)
   - pecas_abaixo_stock_minimo: registos de stock onde quantidade < limite_minimo
   - eficiencia_por_mecanico: só mecânicos com OS concluídas no período;
     ordens_concluidas = len, tempo_medio_minutos = média dos não-None (None se nenhum com tempo)

2. backend/app/routers/dashboard.py

   Router com prefix="/dashboard", tags=["dashboard"].
   Perfis autorizados: ADMINISTRADOR, GERENTE_LOJA.

   Endpoint:
   - GET /dashboard → status 200
     Query params: loja_id, data_inicio, data_fim

3. backend/app/main.py

   Adicionar:
     from app.routers import ..., dashboard
     app.include_router(dashboard.router, prefix="/api/v1")

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/dashboard_service.py backend/app/routers/dashboard.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################

##############################################################################

Prompt 13 — Router + Service: Auditoria

Implementa o router e o service de auditoria do backend DLMCare.

Contexto:
- Schemas em backend/app/schemas/auditoria.py: AuditoriaItemResponse, TipoEventoAuditoria.
- Schemas comuns: PaginatedResponse[T] em schemas/common.py.
- Dependencies: require_roles em app/auth/dependencies.py.
- Contrato em docs/backend_api_contract_etapa3.md, secção 12.
- A auditoria real será integrada nos outros services quando a BD estiver disponível.
  Na Etapa 3, usar uma lista estática em memória com eventos representativos dos 7 tipos.

Regras obrigatórias:
- NÃO criar modelos SQLAlchemy, migrations, SQL, frontend, repositories reais.
- NÃO alterar schemas.

Criar/alterar os seguintes ficheiros:

1. backend/app/services/auditoria_service.py

   _MOCK_AUDITORIA: list[dict] com 7 eventos estáticos, um por tipo de evento:
   LOGIN_SUCESSO, LOGIN_FALHA, ACESSO_NEGADO, OS_ESTADO_ALTERADO,
   STOCK_ENTRADA, STOCK_TRANSFERENCIA, FATURA_EMITIDA.

   Cada evento tem: id, evento (TipoEventoAuditoria), descricao, utilizador_id,
   utilizador_nome, loja_id, ip_origem, timestamp (datetime UTC), detalhe (dict).

   Função:
   - listar(evento, utilizador_id, loja_id, data_inicio, data_fim, page, page_size, current_user)
       -> PaginatedResponse[AuditoriaItemResponse]
     - ADMINISTRADOR: vê tudo, pode filtrar por loja_id
     - GERENTE_LOJA: vê só eventos da sua loja_id
     - Filtros opcionais: evento (exact match em .value), utilizador_id, data_inicio/fim
     - Paginação manual

2. backend/app/routers/auditoria.py

   Router com prefix="/auditoria", tags=["auditoria"].
   Perfis autorizados: ADMINISTRADOR, GERENTE_LOJA.

   Endpoint:
   - GET /auditoria → status 200
     Query params: evento (TipoEventoAuditoria | None), utilizador_id, loja_id,
                   data_inicio, data_fim, page, page_size
   - Passar evento.value (str) ao service, não o enum diretamente.

3. backend/app/main.py

   Adicionar:
     from app.routers import ..., auditoria
     app.include_router(auditoria.router, prefix="/api/v1")

No final da resposta, apresenta:
- Lista de ficheiros criados/alterados com explicação de cada um.
- Comandos para verificar:
  python -m compileall backend/app/services/auditoria_service.py backend/app/routers/auditoria.py
- Tabela de progresso atualizada com ✅ e ❌.

##############################################################################
